on init
    # unlink /efs > /mnt/vendor/efs
    rm /efs
    mkdir /efs
    mkdir /sec_efs
    
on property:ro.vendor.build.id=*
    symlink /vendor/etc/vintf/manifest.xml /vendor/manifest.xml
    restart keystore2
